[package]
name = "dotaemon"
version = "0.1.0"
edition = "2024"
description = "Background daemon of dotboard, delegating all state management away from tui for rapid start-up time"

[dependencies]
bytes = "1.12.0"
dotcore = { path = "../dotcore" }
tokio = { version = "1.52.3", features = ["full"] }
